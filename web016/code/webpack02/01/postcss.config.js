// const autoprefixer = require("autoprefixer")
module.exports = {
    plugins: [
        // 使用autoprefixer 添加css 前缀的标准
        // autoprefixer({
        //     overrideBrowsersList: "last version >1%"
        // })
    ]
}